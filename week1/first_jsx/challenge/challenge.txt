First React With JSX


Create a new React project using create-react-app. Then, change the App.js component so that it shows us a page that resembles the following wireframe. You can also adjust the CSS by changing App.css and using the JSX attribute className.






Create a create-react-app app

Use JSX to display "Hello Dojo"

Use JSX to display a todo list